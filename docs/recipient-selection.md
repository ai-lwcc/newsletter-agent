# Recipient Selection

Recipient selection determines who will receive a campaign.

Rules:

- Must belong to at least one selected target group
- Must have email_consent=True
- Must have a non-empty email address

Duplicate people are removed automatically.

Current phase only previews recipients.

No real emails are sent.